# Cloudflare Pages Configuration
# Este arquivo é usado apenas para referência. 
# A configuração real do Pages está no Cloudflare Dashboard.

# Para Cloudflare Pages, use:
# - Build command: pnpm run build
# - Build output directory: dist/public
# - Deploy command: echo "Deployed via Cloudflare Pages"

# Nota: wrangler.toml não é necessário para Pages.
# Se você quiser usar Workers + Pages, configure-o separadamente.
